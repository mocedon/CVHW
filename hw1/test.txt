import glob
def imageTest(im):
    s0 = 1
    k = np.sqrt(2)
    lvl = [-1, 0, 1, 2, 3, 4]
    th_r = 12
    th_c = 0.03
    
    logs , _ = DoGdetector(im, s0 , k, lvl, th_c, th_r)
    fig, ax = plt.subplots()
    ax.imshow(im, cmap="gray")
    ax.axis('off')
    for log in logs:   
        crcl = plt.Circle((log[0], log[1]), log[2], edgecolor='r', facecolor= 'None')
        ax.add_artist(crcl)
		
for file in glob.glob("../data/*.jpg"):
    im = cv2.imread(file)
    imageTest(im)